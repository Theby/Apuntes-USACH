<%= form_for @expositor, :html => {:class => "form-horizontal", "role" => "form"} do |f| %>
  <% if @expositor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expositor.errors.count, "error") %> prohibited this expositor from being saved:</h2>

      <ul>
      <% @expositor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :nombre, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :publicacion_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.select :publicacion_id, options_for_select(allPublicaciones.map{ |p| [p.nombre, p.id] }), {}, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Actualizar Expositor", :class => "btn btn-default" %>
    </div>
  </div>
<% end %>
