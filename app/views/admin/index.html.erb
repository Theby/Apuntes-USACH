		<section id="title">
			<div class="container">
				<div class="row">
					<div class="col-md-6">
						<h1>Panel de Administración </h1>						
					</div>
				</div>
			</div>
		</section>

		<section id="body">
			<div class="container">
				<h2>1.- Sections</h2>
				<p>
					<%= link_to sections_path, class: "btn btn-default", "role" => "button" do %>
						Ver Sections <span class="badge"> <%= allSections.count %></span>
					<% end %>
				</p>

				<h2>2.- Publicaciones</h2>
				<p>
					<%= link_to publicacions_path, class: "btn btn-default", "role" => "button" do %>
						Ver Publicaciones <span class="badge"> <%= allPublicaciones.count %> </span>
					<% end %>
				</p>

				<h2>3.- Expositores</h2>
				<p>
					<%= link_to expositors_path, class: "btn btn-default", "role" => "button" do %>
						Ver Expositores <span class="badge"> <%= allExpositores.count %> </span>
					<% end %>
				</p>

				<h2>4.- Entries</h2>
				<p>
					<%= link_to entries_path, class: "btn btn-default", "role" => "button" do %>
						Ver Entries <span class="badge"> <%= allEntries.count %> </span>
					<% end %>
				</p>

			</div>
		</section>

		<section id="footer">
			<div class="container navbar-fixed-bottom">
				<div class="panel panel-success">					
					<div class="panel-heading">
						<h3 class="panel-title" align="center"> Desarrollado por Esteban Gaete Flores </h3>
					</div>
				</div>
			</div>
		</section>